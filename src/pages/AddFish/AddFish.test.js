import React from 'react';

describe('AddFish', () => {
  test('displays a form to add a new fish', () => {});

  test('displays a back button to go back to home page', () => {});

  test('displays an error if the form is invalid', () => {});

  test('submits the form if it is valid and redirects to home page', () => {});
});
